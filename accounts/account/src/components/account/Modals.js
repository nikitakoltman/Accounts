import React, { Component } from 'react';

export class Modals extends Component {
    render() {
        return (
            <div>
                modals
            </div>
        )
    }
}

export default Modals
